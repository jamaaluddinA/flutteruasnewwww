name: shopping_list_app
description: A shopping list application with Flutter web
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # HTTP dan state management
  http: ^1.2.1
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.8
  
  # Geolocation
  geolocator: ^11.1.0
  
  # ✅ SUPABASE (gantikan MySQL)
  supabase_flutter: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/